# 认证规则

## 登录

- 登录框支持输入姓名或用户名，按精确文本匹配，前后空格会被去掉。
- 没有密码哈希的旧账号不能登录，需先联系管理员重置密码。
- 数据库密码校验通过后，客户端必须建立与目标账号 ID 一致的 Supabase Auth 会话，才会保存登录状态。
- Supabase Auth 不可达、认证失败或返回的账号 ID 不匹配时，登录失败并清除本地认证状态；RPC 校验成功本身不会建立登录会话。
- 旧账号的数据库密码与 Auth 密码不一致时，客户端会尝试用学号建立 Auth 会话并同步当前密码；同步失败仍会拒绝登录。
- 登录 RPC 对连续失败的账号标识做限流。IP 查询会依次使用多个服务；服务全部不可用时，继续沿用数据库对缺失 IP 的既有处理。

## 账号修改

- 密码和用户名修改只能作用于当前认证用户。
- 修改密码时先验证当前密码，再更新认证密码和应用密码；同步失败会明确提示，不会报告为成功。

## 登录保护

- IP 白名单启用时，能解析出的 IP 继续按白名单限制；客户端缺失或无法解析 IP 时，跳过 IP 限制并继续验证账号密码。

## 数据库部署

线上数据库是运行时权威。检查或调整数据库时，先用本地工具读取当前 schema 和权限；`schema.sql` 只作本机核对，必须保持忽略。任何 `.sql` 文件都不得跟踪或提交，也不要重放或补写历史迁移。登录 RPC 可以匿名调用来验证输入，但客户端只有建立匹配的 Supabase Auth 会话后才算登录成功；其他 RPC 的权限必须按其用途单独核对。
